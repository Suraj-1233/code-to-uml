# Copy to ".env" on the EC2 host (next to docker-compose.prod.yml) and fill in.
# Only DOCKER_USERNAME is read by docker-compose.prod.yml; DOMAIN/EMAIL are used
# by deploy/ec2-setup.sh.

DOCKER_USERNAME=your-dockerhub-username
DOMAIN=codetouml.in
EMAIL=surajkannujiya517@gmail.com
